{
  "name": "booking-calendar-spec",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSpec specification of the Booking Calendar API",
  "type": "module",
  "scripts": {
    "compile": "tsp compile .",
    "compile:watch": "tsp compile . --watch",
    "validate": "redocly lint dist/openapi.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.0",
    "@typespec/compiler": "^1.14.0",
    "@typespec/http": "^1.14.0",
    "@typespec/openapi3": "^1.14.0",
    "@typespec/rest": "^0.84.0"
  }
}
